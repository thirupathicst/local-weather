{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon src/server.js",
    "build": "esbuild src/server.js --bundle --platform=node --outfile=dist/server.js --external:express --external:cors --external:dotenv --external:morgan --external:swagger-jsdoc --external:swagger-ui-express --external:axios",
    "start": "node dist/server.js",
    "start:dev": "node src/server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "axios": "^1.16.1",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "morgan": "^1.10.1",
    "swagger-jsdoc": "^6.2.8",
    "swagger-ui-express": "^5.0.1"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "nodemon": "^3.1.14"
  }
}
